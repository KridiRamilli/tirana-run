# Home screen

Reference: `design/mockups/approved/04-home.png`

- Reference viewport: 390 × 844.
- Preserve the brand header, hero photo, editorial headline, fixed-route summary, three activity stats, primary “View Route” CTA, next-stop card, and bottom navigation.
- Show exactly one route. Do not add recommended routes, carousels, or route discovery.
- Values may use deterministic fixture data for visual tests. Session steps must be labelled as estimated wherever the distinction is explained.
- At 320 CSS pixels, keep the hierarchy and remove horizontal overflow without redesigning the screen.
