import {fireEvent,render,screen} from '@testing-library/react';import {expect,it,vi} from 'vitest';vi.mock('./components/RouteMap',()=>({RouteMap:()=> <div aria-label="Harta e itinerarit të Tiranës"/>}));import {App} from './App';it('shows the fixed route and selects a stop',()=>{render(<App/>);expect(screen.getByRole('heading',{name:'Tirana Historike'})).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:/Piramida/}));expect(screen.getByText('4. Piramida')).toBeInTheDocument()});
